# .npmrc
# 
# 🛑 FIX: This enables a flat node_modules structure (similar to npm/yarn) 
# which fixes module resolution issues for tools like ts-node in a monorepo.
shamefully-hoist=true

# This tells pnpm to symlink workspace packages, which is good practice.
link-workspace-packages=true
